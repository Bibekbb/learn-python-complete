class main:
    def __init__(self, name, age, salary, address, cast):
        name = self.name
        age = self.age
        salary = self.salary
        address = self.address
        cast = self.cast


person = main("Bibek",22,23000,"Ktm","No defined")
print(person.name)